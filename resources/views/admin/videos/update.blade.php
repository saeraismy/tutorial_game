<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Edit Video') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900">
                    <h1 class="mb-0">Edit Video</h1>
                    <hr />
                    <form action="{{ route('admin/videos/update', $videos->kd_videos) }}" method="POST">
                        @csrf
                        @method('PUT')
                        <div class="row">
                            <div class="col mb-3">
                                <label class="form-label">Judul Video</label>
                                <input type="text" name="judul_videos" class="form-control" placeholder="Judul Video" value="{{$videos->judul_videos}}">
                                @error('judul_videos')
                                <span class="text-danger">{{$message}}</span>
                                @enderror
                            </div>
                        </div>
                        <div class="row">
                            <div class="col mb-3">
                                <label class="form-label">Gambar Video</label>
                                <input type="text" name="gambar_videos" class="form-control" placeholder="Gambar Video" value="{{$videos->gambar_videos}}">
                                @error('gambar_videos')
                                <span class="text-danger">{{$message}}</span>
                                @enderror
                            </div>
                        </div>
                        <div class="row">
                            <div class="col mb-3">
                                <label class="form-label">Link Video</label>
                                <input type="text" name="link_videos" class="form-control" placeholder="Link Video" value="{{$videos->link_videos}}">
                                @error('link_videos')
                                <span class="text-danger">{{$message}}</span>
                                @enderror
                            </div>
                        </div>
                        <div class="row">
                            <div class="d-grid">
                                <button class="btn btn-secondary">Update</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
